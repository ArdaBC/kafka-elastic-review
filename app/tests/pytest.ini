[pytest]
minversion = 6.0
addopts = -q
testpaths = app/tests
markers =
    integration: integration tests that use testcontainers (Kafka/Mongo/ES)
    system: system / e2e tests that use docker-compose